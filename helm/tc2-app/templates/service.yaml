apiVersion: v1
kind: Service
metadata:
  name: {{ include "tc2-app.fullname" . }}
  labels:
    {{- include "tc2-app.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}

  selector:
    {{- include "tc2-app.selectorLabels" . | nindent 4 }}

  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http